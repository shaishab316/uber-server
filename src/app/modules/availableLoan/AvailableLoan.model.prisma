model AvailableLoan {
  id        String   @id @default(auto()) @map("_id") @db.ObjectId
  timestamp DateTime @default(now()) @updatedAt

  name          String
  amount        Float
  description   String
  interest_rate Float
  terms         String
  image         String

  loans Loan[]

  @@map("availableLoans")
}
