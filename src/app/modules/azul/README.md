# Azul Payment Module

Azul is a card payment provider that supports 3D Secure 2.0.

## Debug Mode

To enable debug logging for this module, add the following to your `.env` file:

```bash
NODE_DEBUG=app:payment:*
```
